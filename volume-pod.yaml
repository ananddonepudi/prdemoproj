apiVersion: v1
kind: Pod
metadata:
  name: nginxpvc
spec:
  containers:
   - name: nginx
     image: nginx:1.14.2
  
  volumes:
    - name: task-pv-storage
      persistentVolumeClaim:
        claimName: task-pv-claim
